[project]
name = "weatherglass"
version = "1.0.0"
description = "Standalone weather dashboard with configurable location"
requires-python = ">=3.11"
dependencies = [
    "flask[async]>=3.0",
    "httpx[http2]>=0.27",
]

[tool.uv]
dev-dependencies = []
